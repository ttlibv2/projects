package vn.conyeu.javafx.glisten;

/**Defines view stack usage policy. View Stack is either used, skipped or cleared when switching to a new view*/
public enum ViewStackPolicy {
    USE,
    SKIP,
    CLEAR
}